'use client';

import DefaultButton from './default-button';

function ButtonLayout() {
  return (
    <div className="w-full h-full bg-white">
      <DefaultButton text="다음" onClick={() => {}} disabled />
    </div>
  );
}

export default ButtonLayout;
